from spuq.bases.basis import Basis

class TensorProductBasis(Basis):
    def evalAt(self,vector):
        # for b in bases: 
        # side question: what about tensor product bases? Is "vector" a tuple then?
        return NotImplemented
